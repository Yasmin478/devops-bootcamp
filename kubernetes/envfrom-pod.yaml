---
apiVersion: v1
kind: Pod

metadata:
  name: envfrom-demo

spec:
  containers:
    - name: busybox
      image: busybox

      command:
        - sh
        - -c
        - sleep 3600

      envFrom:
        - configMapRef:
            name: app-config 
