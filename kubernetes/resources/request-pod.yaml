---
apiVersion: v1
kind: Pod

metadata:
  name: nginx-request
  namespace: dev

spec:
  containers:
    - name: nginx
      image: nginx:latest

      resources:
        requests:
          cpu: "250m"
          memory: "128Mi"
