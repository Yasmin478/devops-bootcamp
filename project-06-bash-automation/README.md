# Project 06: Bash Automation

## Description

A bash script to create compressed backups of directories

## Features (Current)

- Backup any directory
- Compression using tar.gz
- Timestamp-based backup naming (no overwrite)

## Usage

```
./backup.sh <directory>
```

## Example

```
./backup.sh ~/test_backup
```